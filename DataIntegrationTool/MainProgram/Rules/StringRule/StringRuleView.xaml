<UserControl x:Class="DataIntegrationTool.MainProgram.Rules.StringRule.StringRuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ignore="http://www.galasoft.ch/ignore"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             DataContext="{Binding StringRule,
                                   Source={StaticResource Locator}}"
             mc:Ignorable="d ignore">
    <DockPanel>

        <TextBlock DockPanel.Dock="Top"
                   Style="{StaticResource HeaderTextBlockStyle}"
                   Text="{Binding CurrentlySelectedRuleName}" TextDecorations="Underline"/>
        <TextBlock Text="For this version, all string values for a column on matched records will be concatinated with a pipe (|) delimiter." TextWrapping="Wrap" />
    </DockPanel>
</UserControl>
